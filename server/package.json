{
  "name": "play.mathematicalbasedefenders.com-server",
  "version": "0.5.0-rc.9.2",
  "private": "true",
  "description": "Fun math game",
  "main": "server.js",
  "scripts": {
    "compile": "tsc",
    "copy-static-files": "copyfiles --up 1 \"src/**/*\" -e \"**/*.ts\" dist",
    "start": "npm run compile && npm run copy-static-files && node ./dist/index.js",
    "start-dev": "npm run compile && npm run copy-static-files && node ./dist/index.js"
  }
}
